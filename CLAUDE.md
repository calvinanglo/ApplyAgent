@AGENTS.md

# Rules

## Git — NEVER do this without explicit instruction
- NEVER run `git commit` under any circumstances
- NEVER run `git push` under any circumstances
- NEVER create or delete GitHub repositories
- The user commits and pushes all code themselves
- Only stage files or show diffs when asked
